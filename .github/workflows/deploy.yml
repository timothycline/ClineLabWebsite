# GitHub Actions workflow: build Hugo site and deploy to GitHub Pages
# Triggered on every push to the main branch.
#
# SETUP (one-time, on GitHub.com):
#   1. Go to your repo → Settings → Pages
#   2. Under "Source", choose "GitHub Actions"
#   3. Push to main — this workflow will run automatically.

name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main          # change to "master" if that's your default branch
  workflow_dispatch:  # allows manual trigger from GitHub Actions UI

# Allow the job to deploy to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Only one deployment at a time
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0     # fetch full history for .GitInfo and .Lastmod

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: "latest"
          extended: true      # Blowfish requires extended Hugo

      - name: Cache Hugo modules
        uses: actions/cache@v4
        with:
          path: /home/runner/.cache/hugo_cache
          key: ${{ runner.os }}-hugo-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-hugo-

      - name: Build
        run: hugo --minify --baseURL "${{ steps.pages.outputs.base_url }}/"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
